/*

Functions are structures that allow us to define a code area that can be used more than once
and returns a value

example of a function that returns the value of a number plus other:
*/
function onePlusOther(a, b){
    return a + b;
}
let i = onePlusOther(1,4);
console.log(i);